# Database Configuration
DB_URL=sqlite:///./backend.db

# Qdrant Vector Database Configuration (will be mocked for demo)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=
QDRANT_COLLECTION_NAME=documents

# Redis Configuration (will be mocked for demo)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# SMTP Configuration (will be mocked for demo)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=demo@example.com
SMTP_PASS=demo-password

# OpenAI Configuration (will be mocked for demo)
OPENAI_API_KEY=demo-key

# Embedding Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2
DEFAULT_CHUNKING_METHOD=recursive

# Chunking Parameters
DEFAULT_CHUNK_SIZE=500
DEFAULT_CHUNK_OVERLAP=50

# File Upload Configuration
MAX_FILE_SIZE=10485760
